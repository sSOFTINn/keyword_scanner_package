# README: Програма для пошуку ключових слів у файлах Windows

## 🔧 Крок 1. Встановлення Python
1. Завантаж з офіційного сайту: https://www.python.org/downloads/windows/
2. При встановленні обов’язково увімкни галочку **“Add Python to PATH”**
3. Перевір встановлення командою:
   python --version

## 🛠️ Крок 2. Встановлення бібліотек (разово)
Відкрий PowerShell або CMD і виконай:
   pip install pyinstaller docx PyPDF2 rarfile py7zr tqdm

## ▶️ Крок 3. Запуск програми (GUI через Python)

1. Створи папку, наприклад `keyword_scanner`.
2. Поклади в неї файли:
   - scanner_core.py
   - scanner_gui.py
   - dictionary.txt
3. Відкрий у цій папці PowerShell / CMD і виконай:
   python scanner_gui.py

## ▶️ Крок 4. Збірка в .EXE (GUI)

1. У тій самій папці виконай:
   pyinstaller --onefile --noconsole scanner_gui.py

   (якщо є іконка `scan_icon.ico`, можна вказати так):
   pyinstaller --onefile --noconsole --icon=scan_icon.ico scanner_gui.py

2. Після завершення з’явиться тека `dist/`.
3. У ній буде `scanner_gui.exe`.
4. Скопіюй `dictionary.txt` у ту ж теку, де `scanner_gui.exe`.
5. Запускай `scanner_gui.exe` подвійним кліком.

## 📄 Крок 5. Файли, які створює програма
- scan_log.txt — текстовий лог усіх знайдених результатів (створюється автоматично)
- scan_report.docx — звіт у форматі Word (після завершення сканування)

## 📁 Крок 6. Можливості програми
- Сканування усіх дисків (включно з флешками)
- Пошук ключових слів у:
  - .txt
  - .docx
  - .pdf
  - .zip / .rar / .7z (включно з вкладеними архівами)
- Звітність (TXT + DOCX)
- Вибір мови (UA / EN)
- Прогресбар та GUI-інтерфейс

## 📩 Крок 7. Якщо щось не працює
- Перевір, чи є файл dictionary.txt у папці з програмою
- Переконайся, що антивірус не блокує .exe
- Запускай від імені адміністратора, якщо немає доступу до деяких дисків

Розроблено спеціально для Softin © 2025
